# 📊 Registration & Approval Workflow - Visual Diagrams

## Workflow 1: Tenant Registration & Approval

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                       TENANT REGISTRATION WORKFLOW                          │
└─────────────────────────────────────────────────────────────────────────────┘

     USER SIDE                    SYSTEM SIDE                  MANAGER SIDE
     
┌──────────────┐
│ Register Form├──────────────────────────────────────────────┐
│  - Name      │                                              │
│  - Email     │    ┌─────────────────────────────────────┐   │
│  - Password  │    │ Create in auth.users                │   │
│  - Property  ├───→│ (Trigger fires)                     │   │
│  - Unit      │    │                                     │   │
└──────────────┘    │ ├─ Insert into profiles             │   │
                    │ │  (status='pending')               │   │
                    │ ├─ Update unit status='reserved'    │   │
                    │ ├─ Create approval_request          │   │
                    │ └─ Send notification                │   │
                    └─────────────────────────────────────┘   │
                                   ↓                           │
                    ┌──────────────────────────────┐          │
                    │ Show: "Awaiting Verification" │          │
                    └──────────────────────────────┘          │
                                   ↓                           │
                                   ├──────────────────────────→ ┌──────────────────┐
                                   │                           │ Notification      │
                                   │                           │ - Pending tenant  │
                                   │                           │ - Unit details    │
                                   │                           │ - Approve button  │
                                   │                           └──────────────────┘
                                   │                                    │
                                   │                                    ↓
                                   │                           ┌──────────────────┐
                                   │                           │ Manager Reviews  │
                                   │                           │ - Tenant info    │
                                   │                           │ - Unit details   │
                                   │                           │ - Accept/Reject  │
                                   │                           └──────────────────┘
                                   │                                    │
                                   │                                    ↓
                                   │            ┌──────────────────────┘
                                   │            │
                                   │            ├─ Update profiles.status='active'
                                   │            ├─ Update approval_request.status='approved'
                                   │            └─ Send notification to tenant
                                   │
                                   ↓
                    ┌──────────────────────────────┐
                    │ Can Now Login!               │
                    │ Redirect to Tenant Dashboard │
                    └──────────────────────────────┘

Database State Changes:
  profiles:            status='pending' → status='active'
  approval_requests:   status='pending' → status='approved'
  units_detailed:      status='vacant'  → status='reserved'
  notifications:       New record with type='tenant_verification'
```

---

## Workflow 2: Property Manager Registration & Approval

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    PROPERTY MANAGER REGISTRATION WORKFLOW                   │
└─────────────────────────────────────────────────────────────────────────────┘

     USER SIDE                    SYSTEM SIDE                  ADMIN SIDE
     
┌──────────────────┐
│ Register Form    │
│  - Name          │
│  - Email         │
│  - Password      ├───────────────────────────────────────────┐
│  - Properties    │     ┌──────────────────────────────────┐  │
│    (multi-select)│     │ Create in auth.users             │  │
└──────────────────┘     │ (Trigger fires)                  │  │
                         │                                  │  │
                         │ ├─ Insert into profiles          │  │
                         │ │  (status='pending')            │  │
                         │ ├─ Create approval_request       │  │
                         │ │  (type='manager_assignment')   │  │
                         │ └─ Send notification to super    │  │
                         │    admin                         │  │
                         └──────────────────────────────────┘  │
                                  ↓                            │
                         ┌──────────────────────────┐          │
                         │ Show: "Awaiting Approval" │          │
                         └──────────────────────────┘          │
                                  ↓                            │
                                  ├───────────────────────────→ ┌──────────────────┐
                                  │                            │ Super Admin       │
                                  │                            │ Notification      │
                                  │                            │ - Manager name    │
                                  │                            │ - Properties      │
                                  │                            │ - Approve button  │
                                  │                            └──────────────────┘
                                  │                                     │
                                  │                                     ↓
                                  │                            ┌──────────────────┐
                                  │                            │ Admin Reviews    │
                                  │                            │ - Manager details│
                                  │                            │ - Properties list│
                                  │                            │ - Accept/Reject  │
                                  │                            └──────────────────┘
                                  │                                     │
                                  │                                     ↓
                                  │                  ┌──────────────────┘
                                  │                  │
                                  │                  ├─ Update profiles.status='active'
                                  │                  ├─ Update approval_request.status='approved'
                                  │                  └─ Send notification to manager
                                  │
                                  ↓
                         ┌──────────────────────────┐
                         │ Can Now Login!           │
                         │ Redirect to Manager      │
                         │ Dashboard               │
                         └──────────────────────────┘

Database State Changes:
  profiles:            status='pending' → status='active'
  approval_requests:   status='pending' → status='approved'
  notifications:       New record with type='manager_approval'
```

---

## Database Schema Overview

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         REGISTRATION SYSTEM TABLES                          │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────┐
│     auth.users          │
├─────────────────────────┤
│ id (UUID)              │◄────────┐
│ email                  │         │
│ password_hash          │         │
│ created_at             │         │
│ raw_user_meta_data     │         │
│  ├─ full_name          │         │
│  ├─ phone              │         │
│  ├─ role               │         │
│  └─ status             │         │
└─────────────────────────┘         │
                                   │
                      TRIGGER: handle_new_user()
                      (SECURITY DEFINER)
                                   │
                                   │
┌─────────────────────────┐         │
│   public.profiles       │         │
├─────────────────────────┤         │
│ id (UUID) ─────────────────── PRIMARY
│ email (UNIQUE)         │
│ full_name              │
│ phone                  │
│ role ─────────────────────► Determines access level
│  ├─ 'tenant'           │   (tenant_verification pending)
│  ├─ 'property_manager' │   (manager_assignment pending)
│  └─ 'super_admin'      │   (no approval needed)
│ status ───────────────────► Controls login access
│  ├─ 'pending'          │   ❌ Cannot login until approved
│  ├─ 'active'           │   ✅ Can login
│  ├─ 'inactive'         │   ❌ Disabled
│  └─ 'suspended'        │   ❌ Blocked
│ property_id            │
│ unit_id                │
│ created_at             │
│ updated_at             │
└─────────────────────────┘
         ▲
         │
         │ Referenced by
         │
┌─────────────────────────────────────────┐
│    approval_requests                    │
├─────────────────────────────────────────┤
│ id (UUID)                               │
│ submitted_by ────→ profiles.id (applicant)
│ type ──────────────► 'tenant_verification'
│                    or 'manager_assignment'
│ title              │ Readable title
│ description        │ Full details
│ property_id        │ For tenants
│ unit_id            │ For tenants
│ status ────────────► 'pending' / 'approved' / 'rejected'
│ created_at         │
│ updated_at         │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│    notifications                        │
├─────────────────────────────────────────┤
│ id (UUID)                               │
│ recipient_id ──────→ profiles.id (approver)
│ sender_id ─────────→ profiles.id (applicant)
│ type ──────────────► 'tenant_verification'
│                    or 'manager_approval'
│ related_entity_id ──► profiles.id (applicant)
│ title              │ "New Tenant Registration"
│ message            │ Details
│ is_read            │ false initially
│ created_at         │
└─────────────────────────────────────────┘

┌─────────────────────────┐
│   units_detailed        │
├─────────────────────────┤
│ id (UUID)              │
│ property_id            │
│ unit_number            │
│ unit_type              │
│ floor_number           │
│ price_monthly          │
│ occupant_id ──────────────────► profiles.id
│ status                 │
│  ├─ 'vacant'           │ Available
│  ├─ 'occupied'         │ Has tenant lease
│  ├─ 'maintenance'      │ Being repaired
│  └─ 'reserved' ◄─────────────── Set when tenant registers
│ created_at             │
│ updated_at             │
└─────────────────────────┘
```

---

## RLS Policy Hierarchy (Fixed)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                   Row-Level Security (RLS) FLOW                             │
└─────────────────────────────────────────────────────────────────────────────┘

When user tries to INSERT profile:
┌─────────────────┐
│ Auth.users      │ Signup
│ INSERT complete │
└────────┬────────┘
         │
         ▼
┌──────────────────────┐
│ Trigger fires        │
│ on_auth_user_created │
│ SECURITY DEFINER ◄───── KEY FIX: Bypasses RLS!
└────────┬─────────────┘
         │
         ▼
┌──────────────────────────────┐
│ Check RLS Policy             │
│ ✓ Service role can manage    │  ◄─── NEW: Simple check
│   profiles                   │       (Not recursive!)
└────────┬─────────────────────┘
         │
         ▼
┌──────────────────────────────┐
│ INSERT into profiles         │
│ - id: user.id              │
│ - email: user.email        │
│ - status: 'pending'        │
│ - role: from metadata      │
│ SUCCESS! ✅                 │
└──────────────────────────────┘


OLD WAY (Broken - DO NOT USE):
┌──────────────────────────────┐
│ RLS Policy Check             │
│ SELECT * FROM profiles       │  ◄─── Circular: Profile doesn't exist yet!
│ WHERE role='super_admin'     │       ┌─ If super_admin doesn't exist
│                              │       │  in profiles, query fails
└──────────────────────────────┘       │
     ┌──────────────────────────────────┘
     ▼
┌──────────────────────────────┐
│ 500 ERROR ❌                 │
│ "Database error             │
│  saving new user"           │
└──────────────────────────────┘

```

---

## Summary Table

| Component | Before (Broken) | After (Fixed) |
|-----------|-----------------|----------------|
| **RLS Policy** | Checks user role by querying profiles table | Simple policy: service_role can manage |
| **Trigger** | `SECURITY INVOKER` (limited permissions) | `SECURITY DEFINER` (elevated permissions) |
| **Profile Creation** | INSERT then UPDATE (complex) | Just UPDATE (trigger creates profile) |
| **Approval Table** | `manager_approvals` (custom) | `approval_requests` (standard) |
| **Error Handling** | Complex catch logic | Simple warnings, don't block signup |
| **Result** | 500 Error ❌ | Successful registration ✅ |

