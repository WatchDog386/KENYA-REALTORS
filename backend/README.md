# Backend Folder Status

This folder is intentionally kept in the repository for clean structure and future expansion.

Current runtime mode:

- No active Express server.
- Application logic runs from repository root frontend.
- Database/auth/storage access is direct through Supabase.

Run the project from repository root:

```bash
npm run dev
```

Build from repository root:

```bash
npm run build
```
